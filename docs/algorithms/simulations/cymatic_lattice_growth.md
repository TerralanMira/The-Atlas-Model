# Cymatic Lattice Growth

Standing-wave drive on a 2D grid with threshold deposition (|u| > thresh) → patterns.

**Run**
```bash
python -m sims run cymatic_lattice_growth --nx 96 --ny 96 --omega 2.0
